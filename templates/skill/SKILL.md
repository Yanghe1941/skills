---
name: your-skill-name
description: One or two sentences on what it does, then when to use it — list concrete trigger phrases. 中文一句话功能 + 触发场景（例：用户提到「……」「……」时）。
---

<!-- description: no colon followed by a space, or wrap the whole value in quotes — otherwise YAML fails and installers skip the skill.
     description 里不要出现「英文冒号+空格」，否则 YAML 解析失败，安装器会跳过该技能。 -->

# Skill Title / 技能名

One line: the problem this skill solves, and the principle behind it.
一句话：这个技能解决什么问题，背后的原则是什么。

## When to activate / 何时启用

| Situation / 场景 | What to do / 做法 |
|---|---|
| … | … |

## Steps / 步骤

1. …
2. …

## Output / 输出

| Column / 列 | Column / 列 |
|---|---|

## Guardrails / 边界

- What this skill must never do (delete, overwrite, publish without confirmation …).
  绝对不能做的事（删除、覆盖、未经确认就发布……）。
- What needs explicit user confirmation first.
  哪些操作必须先得到用户明确确认。
